# MongoDB Atlas connection string
MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/senim?retryWrites=true&w=majority"

# OpenAI API key
OPENAI_API_KEY="sk-..."

# Optional: model override (defaults to gpt-4o-mini)
OPENAI_MODEL="gpt-4o-mini"
